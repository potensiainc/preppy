# STAGING 공개 데이터 준비와 내 프레피 검증 — 2026-09-26

## 대상과 원인
- 프로젝트 preppy-production의 staging / preppy-web-staging.
- 실제 웹 DATABASE_URL의 데이터베이스 이름은 preppy_staging이다. 같은 Postgres-nu8d 서비스의 기본 railway DB와 다르므로 기본 DB 이름을 추정하면 안 된다.
- 이전 단계의 기본 railway DB 기준 마이그레이션 완료 기록은 실제 웹 DB 상태를 보증하지 않는다. 이번에 실제 preppy_staging DB에서 확인한 migration ledger는 17개다. 자동 탈퇴 활성화 전 0017 마이그레이션을 정확한 DB에 적용/검증해야 한다. 자동 탈퇴는 계속 비활성 상태다.
- 변경 전: 기관 22개(국제학교/DRAFT), 공개 입학정보 0개, 활성 모니터링 설정 0개, 가입 회원 1명.
- 운영 공개 기관은 189개지만 활성 모니터링 설정 0개. 단순 DB 복사만으로 관심기관 등록 조건은 충족되지 않는다.

## 반영
- 공개 기관 189개: 영어유치원 126, 사립초등학교 41, 국제학교 22.
- 공개 NATIVE 입학정보 69개, 현재 검증 버전과 근거 연결, 기관 사실/공식 출처/확인 상태 복사.
- 운영 DB에는 READ ONLY 트랜잭션으로만 접근. 쓰기 없음.
- 회원·로그인·동의·알림 발송·운영 관리자 테이블은 복사하지 않음.
- 관리자 검수자 ID, 내부 메모, 원본 파일·본문·스토리지 키와 응답 메타데이터는 복사 제외. 표시 사실·학년도·조건·verified_at/collected_at은 보존.
- 최초 모의 실행 전체 롤백 후 적용. 각 단계 자연키에 따라 ID 재매핑. 참조 무결성 검증.
- 기존 회원 관련 10개 테이블의 내용 해시를 변경 전후 비교하여 그대로임을 확인.
- 반영 전 공개 테이블 백업: .runtime-temp/staging-catalog-20260926/before-public-catalog.json.
- 재실행 모의 검증: 새 행 삽입 0개, 중복 없음, 회원 데이터 보존.
- 검증된 입학정보/기관 사실에 연결된 공식 출처 189개에 STAGING 전용 MANUAL 설정 추가. 실제 수집 작업/이메일/분석은 비활성화 유지.

## 등록 가능 범위
- 기존 등록 정책을 완화하지 않음.
- 등록 가능: 사립초등학교 41개 / 국제학교 2개(서울외국인학교, 아시아퍼시픽국제외국인학교).
- 영어유치원 126개는 목록/상세 확인이 가능하지만 현재 검증된 기관 사실/입학정보를 통한 출처 연결이 없어 등록 조건 미충족. 현재 전용 상세 화면에는 등록 CTA도 없음.
- 향후 영어유치원의 관심기관 저장을 제공하려면 공식 정보 확인과 기존 출처 연결 정책/전용 상세 화면의 기능 검토가 별도로 필요하다. 가짜 VERIFIED 사실을 만들어 통과시키지 않았다.

## 실제 STAGING 브라우저 검증
- 사용자 계정 대신 일회용 별도 QA 계정 사용. 외부 카카오 가입/토큰교환은 이번 검증 대상이 아님.
- 빈 목록 안내 → 경기초등학교 등록 → 내 프레피 목록 표시 → 상세 이동 PASS.
- 서울외국인학교 추가 → 두 기관 목록 PASS.
- 새로고침 후 두 기관 유지 PASS.
- 서울외국인학교 해제 확인 → 목록 제거 PASS.
- 로그아웃 → 동일 QA 계정의 새 서버 세션으로 재접속 → 경기초등학교 유지 / 해제한 기관 제외 PASS.
- 서울외국인학교 재등록 PASS.
- 390px 모바일 화면 가로 넘침 없음.
- 기존 날짜가 지난 정보는 최신 모집으로 바꾸지 않음. 목록은 현행 정책상 OPEN/UPCOMING 위주이므로 과거 모집만 있는 학교는 현재 입학 상태가 미확인으로 표시될 수 있음.

## 화면 수정
- 내 프레피 국제학교 카드의 지역 코드 11/41이 그대로 노출되는 문제 발견.
- 표시만 서울/경기로 변환. 원본 기관 지역 값은 변경하지 않음.
- 수정 전 회귀 검증 2개 실패 재현, 수정 후 내 프레피/등록 자격 관련 27개 PASS.
- TypeScript 및 프로덕션 빌드 PASS.

## 재사용 도구
- scripts/staging/export-public-catalog.cjs: 공개 데이터 허용 목록, read-only 스냅샷.
- scripts/staging/upload-public-catalog.py: 특정 STAGING 컨테이너에 압축본 분할 전송.
- scripts/staging/import-public-catalog.cjs: env/DB 호스트/DB명 제한, 모의 실행 기본값, --apply 시에만 commit, 회원 내용 해시 보존 검증.
- scripts/staging/enable-manual-test-sources.cjs: 검증 근거가 있는 공식 출처의 STAGING 수동 설정.
- scripts/staging/verify-public-catalog.cjs: 실제 공개 수/등록 가능 수 확인.
- 날짜가 포함된 파일 경로는 이번 스냅샷을 가리킨다. 향후 실행 시 새 스냅샷으로 갱신해야 한다.

## UX Writing
UX Writing: PASS — 이번 작업의 지역 표시 및 관심기관 등록·빈 목록·해제·재접속 상태 범위. 서울/경기의 의미를 보존하고 숫자 코드 노출을 제거. 공식 내용·날짜·불확실성은 바꾸지 않음. 제공하지 않는 이메일 발송을 제공한다고 표시하지 않음. 영어유치원의 기존 화면 전체 문안을 검수/승인했다는 뜻은 아님.

## 최종 결과
- 지역명 수정 배포 7d0af40c-ad6e-4086-b765-d362fdbf3a2e: SUCCESS.
- 배포 후 실제 STAGING: 국제학교 · 서울 표시, 숫자 11 노출 없음, 390px 가로 넘침 없음.
- Runtime image contract PASS는 이미지 파일 계약 검증이며 실제 DB migration 적용 검증을 대체하지 않음.
- 이번 QA 계정 및 관련 관심기관/에피소드 삭제 완료. 기존 회원 1명 유지, 공개 기관 189개/입학정보 69개 유지.
- 카카오 재인증 자체를 대신 수행하지 않음. 실제 로그아웃 후 새 서버 인증 세션을 발급해 저장 상태 유지 여부를 검증함.
- 사용자는 /institutions/kyonggi 또는 /institutions/seoul-foreign-school에서 관심기관 등록 후 /my-preppy를 확인할 수 있음.
